[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
homeassistant = {version = "*", markers = "platform_system != 'Darwin'"}

[dev-packages]
pytest = "*"
pytest-asyncio = "*"
# Bleak pulls in pyobjc on macOS only via CoreBluetooth backend. Ensure we never attempt PyObjC on Linux CI.
# Use environment marker to only install Bleak on non-Darwin, and add a no-op stub on Darwin for clarity.
bleak = {version = "*", markers = "platform_system != 'Darwin'"}
ruff = "*"
black = "*"

[requires]
python_version = "3.13"
